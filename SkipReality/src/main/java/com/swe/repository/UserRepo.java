/*package com.swe.repository;

import org.springframework.data.repository.CrudRepository;
import java.util.List;
import com.swe.entity.User;


public interface UserRepo extends CrudRepository<User, Integer> {
	
	//Check System Users
	//List<User> FindUserByUserPasswordAndName(String Name, String Password);
	//User FindUserByUserPasswordAndName(String Name, String Password);
	//Iterable<User> findAll();
	//User findOne(String Name, String Password);
	//public Iterable<User> findByNameContainingIgnoreCase(String Name,String Password);

}


spring.jpa.hibernate.ddl-auto=create
spring.datasource.url=jdbc:mysql://localhost:3306/myproject
spring.datasource.username=root
spring.datasource.password=

*/

    